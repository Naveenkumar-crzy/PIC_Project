#pragma config FOSC = HS        // Oscillator Selection bits (HS oscillator)
#pragma config WDTE = OFF       // Watchdog Timer Enable bit (WDT disabled)
#pragma config PWRTE = OFF      // Power-up Timer Enable bit (PWRT disabled)
#pragma config BOREN = ON       // Brown-out Reset Enable bit (BOR enabled)
#pragma config LVP = ON         // Low-Voltage (Single-Supply) In-Circuit Serial Programming Enable bit (RB3/PGM pin has PGM function; low-voltage programming enabled)
#pragma config CPD = OFF        // Data EEPROM Memory Code Protection bit (Data EEPROM code protection off)
#pragma config WRT = OFF        // Flash Program Memory Write Enable bits (Write protection off; all program memory may be written to by EECON control)
#pragma config CP = ON          // Flash Program Memory Code Protection bit (All program memory code-protected)

// #pragma config statements should precede project file includes.
// Use project enums instead of #define for ON and OFF.

#include <xc.h>
#define _XTAL_FREQ 20000000

#define RS PORTBbits.RB0
#define RW PORTBbits.RB1
#define EN PORTBbits.RB2

#define C1 PORTBbits.RB5
#define C2 PORTBbits.RB6
#define C3 PORTBbits.RB7

#define R1 PORTDbits.RD0
#define R2 PORTDbits.RD1
#define R3 PORTDbits.RD2
#define R4 PORTDbits.RD3



void lcd_data(unsigned char data)
{
    PORTC = data;
    RS = 1;
    RW = 0;
    EN = 1;
    __delay_ms(5);
    EN = 0;
}
void lcd_cmd(unsigned char cmd )
{
    PORTC = cmd;
    RS = 0;
    RW = 0;
    EN = 1;
    __delay_ms(5);
    EN = 0;
}
void lcd_string (unsigned char *str ,unsigned char num)
{
    for (unsigned int i = 0;i < num;i++)
    {
        lcd_data (str[i]);
    }
}

void lcd_init()
{
    lcd_cmd (0x38);
    lcd_cmd (0x06);
    lcd_cmd (0x0C);
    lcd_cmd (0x01);
}
int keypad()
{
    while (1) 
    {
    C1 =1 ; C2 = 0; C3 =0 ;
    
    if (R1 == 1)
    {
     
        while (R1 ==1);
        return 1;
    }
    if (R2 == 1)
    {
   
        while (R2 ==1);
         return 4;
    }
    if (R3 == 1)
    {
       
        while (R3 ==1);
         return 7;
        
    }
    if (R4 == 1)
    {
      
        while (R4 ==1);
         return 0;
    }
     C1 =0; C2 = 1; C3 =0 ;
    
    if (R1 == 1)
    {
      
        while (R1 ==1);
         return 2;
    }
    if (R2 == 1)
    {
      
        while (R2 ==1);
         return 5;
    }
    if (R3 == 1)
    {
      
        while (R3 ==1);
         return 8;
    }
    if (R4 == 1)
    {
       
        while (R4 ==1);
         return 0;
    }
     C1 =0; C2 = 0; C3 =1 ;
    
    if (R1 == 1)
    {
       
        while (R1 ==1);
         return 3;
    }
    if (R2 == 1)
    {
      
        while (R2 ==1);
         return 6;
    }
    if (R3 == 1)
    {
       
        while (R3 ==1);
     return 9;
    }
    if (R4 == 1)
    {
       
        while (R4 ==1);
         return 0;
    }
  }
}



void main() {
    TRISB= 0x00;   // port B output
    TRISC = 0x00;   // port C output
    TRISD = 0xFF; // port D input
   unsigned   int a,b,c,d;
    
    lcd_init();
    lcd_cmd(0x81);
    lcd_string ("ENTER PASSWORD",14);
    lcd_cmd(0xC5);
    
   
    while (1)
    {
        a=keypad();  // store the value in variable 
        lcd_data('*');
        b=keypad();
        lcd_data('*');
        c=keypad();
        lcd_data('*');
        d=keypad();
        lcd_data('*');   // password enter star print  
        
        if (a==1 && b == 2 && c ==3 && d == 4)   // condition checking 
        {
            lcd_cmd (0x01);
             lcd_cmd(0x81); 
            lcd_string ("PASSWORD DONE!",15); 
               __delay_ms(3000);
        }
        else 
            {
            lcd_cmd (0x01);
             lcd_cmd(0x81);       // for sec0nd row 
            lcd_string ("WRONG PASSWORD",15); 
            __delay_ms(3000);
          break;                  // if password is wrong break the loop start from first
        }
      
    }
    
   
}



